% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz.R
\name{use_catppuccin}
\alias{use_catppuccin}
\title{Activate Catppuccin Defaults for Common Geoms and Discrete Scales}
\usage{
use_catppuccin(flavor = getOption("ggcatppuccin.flavor", "mocha"))
}
\arguments{
\item{flavor}{Which Catppuccin flavor to use ("mocha", "latte", "frappe",
"macchiato", "mocha_cb", "latte_cb", "frappe_cb", "macchiato_cb").
Defaults to the value set by \code{\link[=set_catppuccin_flavor]{set_catppuccin_flavor()}}, or "mocha" if not
set.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effects.
}
\description{
Sets Catppuccin colors as the default for common geoms (point, line, bar, col,
boxplot, violin) and as the default discrete color/fill scales. After calling
this, plain \code{geom_line()} and mapped discrete \code{color}/\code{fill} aesthetics will
automatically use Catppuccin colors, for those supported defaults.
}
\examples{
library(ggplot2)

use_catppuccin("mocha")

# Now plain geoms use Catppuccin colors:
ggplot(mtcars, aes(mpg, wt)) + geom_point()

# And mapped aesthetics auto-use the Catppuccin palette:
ggplot(mtcars, aes(mpg, wt, color = factor(cyl))) + geom_point()

# Restore original defaults when done:
reset_catppuccin()
}
\seealso{
\code{\link[=reset_catppuccin]{reset_catppuccin()}} to restore ggplot2 defaults.
}
